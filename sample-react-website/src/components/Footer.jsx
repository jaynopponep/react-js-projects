function Footer(){
    return(
        <footer>
            <p>&copy; {new Date().getFullYear()} Papa's Pizzeria</p>
        </footer>
    );
}

export default Footer;
// %copy = Copyright symbol, new Date().getFullYear shows current year.
